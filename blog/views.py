# from django.shortcuts import render, redirect
# from wagtail.models import Page
# # Create your views here.
# def blog_index_redirect(request):
#     # Find the blog index page and redirect
#     blog_index_page = Page.objects.get(slug='blog')
#     return redirect(blog_index_page.url)